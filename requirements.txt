fastapi>=0.110,<0.112
uvicorn[standard]>=0.27,<1.0

# config / env
python-dotenv>=1.0,<2.0
pydantic>=2.0,<2.8
pydantic-settings>=2.2,<2.8

# auth / security
passlib[bcrypt]>=1.7.4,<2.0
python-jose[cryptography]>=3.3,<4.0

# database (SQLite via SQLAlchemy)
sqlalchemy>=2.0,<3.0

# validation / utilities (FastAPI already uses these internally)
anyio>=4.4,<5.0
click>=8.1,<9.0
annotated-types>=0.6,<1.0
typing-extensions>=4.8,<5.0